# {{ ansible_managed }}
/srv/**/log/*.log {
  su root root
  daily
  size 50M
  rotate 3
  missingok
  notifempty
  delaycompress
  copytruncate
  {% if common_restart_rsyslog %}
  postrotate
    reload rsyslog >/dev/null 2>&1 || true
  endscript
  {% endif %}
}
